en el contexto la demostracion de cada fact vale
con contexto el prefijo estricto anterior (sin incluirlo)

estructura:
ND
    form, proof

NDChecker
    check

theory (a futuro nombre de lenguaje)
    data de programa

Certifier
    certify
    check

--

axiom a: P
theorem b: P
proof
    thus p by a
qed

--

agrear a los términos una metavar con un número para instanciar en meta variables frescas

que tiene que hacer by?
las reglas de DN que no tenes, y subjetivamente "ergonómico"
intro y elim de Y, elim de imp, 

DNF es completa para proposicional, y le metemos una heurística para los para todos
    de forma semántica: es contradicción sii hay un literal y su negación en cada cláusula, entonces siempre lo vas a encontrar

dual a resolución? de dónde se sale?

--

solve

para refutar una cláusula tenés que encontrar sin emitir demo las dos que son negadas y después emitir la dmeo con N and-is

--

DNF

cut:

G |- A          G, A |- B
-------------------------- (cut)
        G |- B

alt big step y small step (reescritura)

pseudo DNF con cuantificadores como cajas negras

se tratan como literales, para después poder unificar>

--

considerar peinar los or e ys, agregar estas dos reglas

a y (b y c) -> (a y b) y b
a o (b o c) -> (a o b) o b

capaz devolver dos proofs, A |- B y B |- A
para usar que los conectivos no son monótonos pero si presevan el sii con la negación 

en el no se dan vueltas las cosas, se invierte en papel de las dos pruebas.